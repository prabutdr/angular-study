let count = 5;

// count = 'a';

let a: number;
let b: boolean;
let c: string;
let d: any;
let e: number[] = [1, 2, 3];
let f: any[] = [1, true, 'test'];

const ColorRed = 0;
const ColorGreen = 1;
const ColorBlue = 2;

enum Color { Red = 1, Green, Blue };
let backgroundColor = Color[0];
console.log(backgroundColor);